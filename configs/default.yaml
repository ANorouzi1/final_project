data:
  dataset_root: data/ftw
  image_size: 512
  batch_size: 8
  num_workers: 4

train:
  epochs: 50
  learning_rate: 1.0e-4
  weight_decay: 1.0e-5
  aux_loss_weight: 0.5

model:
  backbone: unet
  encoder_init: scratch
